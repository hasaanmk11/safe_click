# SafeClick

A Flutter package to prevent double taps or rapid clicks on any widget.

## Features

- Works with any widget: `Container`, `ElevatedButton`, `IconButton`, etc.
- Set a custom cooldown duration
- Optional visual feedback during cooldown

## Installation

Add this to your `pubspec.yaml`:

```yaml
dependencies:
  safe_click: ^0.0.1
```
